[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback (harmless with HashRouter; safe if you switch to BrowserRouter later)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
